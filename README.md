# 🗄️ SQL Commands Guide

Este repositório reúne exemplos práticos, scripts e anotações sobre comandos SQL (**Structured Query Language**). O objetivo é servir como um guia de consulta rápida para revisar e aprender sobre manipulação e definição de dados.

## 📋 Estrutura do Repositório

Os comandos estão organizados de acordo com suas categorias funcionais:

### 1. DDL (Data Definition Language)
Comandos responsáveis por definir a estrutura do banco de dados (esquemas, tabelas, views).
* `CREATE`: Criar novos objetos.
* `ALTER`: Modificar a estrutura existente.
* `DROP`: Excluir objetos.
* `RENAME` & `COMMENT`.

### 2. DML (Data Manipulation Language)
Comandos utilizados para manipular os dados dentro das tabelas.
* `INSERT`: Inserir novos dados.
* `UPDATE`: Atualizar dados existentes.
* `DELETE`: Remover dados.

### 3. DQL (Data Query Language)
Focado na consulta e recuperação de dados.
* `SELECT`: O comando principal para ler dados, incluindo cláusulas como `WHERE`, `ORDER BY`, `GROUP BY` e `JOINs`.

### 4. DCL (Data Control Language) & TCL (Transaction Control Language)
*(Opcional/Em desenvolvimento)*
* Controle de acesso (`GRANT`, `REVOKE`) e transações (`COMMIT`, `ROLLBACK`).
